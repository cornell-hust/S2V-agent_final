# 任务：独立化 idea2_v3 仓库

## 目标
让 idea2_v3 完全不依赖 idea2_v2。复制 4 个缺失脚本，修改 import 路径。

## 脚本 1: build_frame_cache.py (190行)

```bash
cp /mnt/shared-storage-user/mineru2-shared/zengweijun/Wmh/ideas/idea2_v2/code/build_frame_cache.py \
   /mnt/shared-storage-user/mineru2-shared/zengweijun/Wmh/ideas/idea2_v3/build_frame_cache.py
```

import 替换:
| 旧 | 新 |
|----|-----|
| `from saver_agent.dataset` | `from saver_v3.data.dataset` |

## 脚本 2: build_feature_cache.py (190行)

```bash
cp /mnt/shared-storage-user/mineru2-shared/zengweijun/Wmh/ideas/idea2_v2/code/build_feature_cache.py \
   /mnt/shared-storage-user/mineru2-shared/zengweijun/Wmh/ideas/idea2_v3/build_feature_cache.py
```

import 替换:
| 旧 | 新 |
|----|-----|
| `from saver_agent.dataset` | `from saver_v3.data.dataset` |
| `from saver_agent.proposal` | `from saver_v3.core.proposal` |

## 脚本 3: annotate_teacher_judge_sft.py (1076行)

```bash
cp /mnt/shared-storage-user/mineru2-shared/zengweijun/Wmh/ideas/idea2_v2/code/annotate_teacher_judge_sft.py \
   /mnt/shared-storage-user/mineru2-shared/zengweijun/Wmh/ideas/idea2_v3/annotate_teacher_judge_sft.py
```

import 替换:
| 旧 | 新 |
|----|-----|
| `from saver_agent.prepared_metadata` | `from saver_v3.data.prepared_metadata` |
| `from saver_agent.environment` | `from saver_v3.core.environment` |
| `from saver_agent.proposal` | `from saver_v3.core.proposal` |
| `from saver_agent.runtime` | `from saver_v3.common.runtime` |
| `from saver_agent.teacher_judge` | `from saver_v3.teacher.teacher_judge` |
| `from saver_agent.training import` | `from saver_v3.sft.training import` |
| `from saver_agent.training_data` | `from saver_v3.data.training_data` |

## 脚本 4: build_saver_data.py (278行)

```bash
cp /mnt/shared-storage-user/mineru2-shared/zengweijun/Wmh/ideas/idea2_v2/code/build_saver_data.py \
   /mnt/shared-storage-user/mineru2-shared/zengweijun/Wmh/ideas/idea2_v3/build_saver_data.py
```

import 替换:
| 旧 | 新 |
|----|-----|
| `from saver_agent.config` | `from saver_v3.data.config` |
| `from saver_agent.prepared_metadata` | `from saver_v3.data.prepared_metadata` |
| `from saver_agent.proposal` | `from saver_v3.core.proposal` |
| `from saver_agent.runtime` | `from saver_v3.common.runtime` |
| `from saver_agent.training` | `from saver_v3.sft.training` |
| `from train_saver_sft import` | `from saver_v3.sft.training import` |

## 清理

```bash
rm -rf /mnt/shared-storage-user/mineru2-shared/zengweijun/Wmh/ideas/idea2_v3/third_party_ports/v2/
```

保留 `third_party_ports/timesearch_r/`（真正第三方代码）。

## 更新文档

更新 `docs/预处理命令.md`：所有命令中的 v2 路径改为 v3 本地路径。

## 验收标准

```bash
cd /mnt/shared-storage-user/mineru2-shared/zengweijun/Wmh/ideas/idea2_v3

# 1. 语法检查
for f in build_frame_cache.py build_feature_cache.py annotate_teacher_judge_sft.py build_saver_data.py; do
  python3 -m py_compile "$f" && echo "OK: $f" || echo "FAIL: $f"
done

# 2. 无 v2 依赖
grep -rn "idea2_v2" *.py saver_v3/ scripts/ 2>/dev/null | grep -v __pycache__
# 应返回 0 结果

# 3. 无残留 saver_agent import
grep -rn "from saver_agent\b" *.py saver_v3/ 2>/dev/null | grep -v __pycache__
# 应返回 0 结果

# 4. 关键 import
python3 -c "from saver_v3.core.reward import score_rollout_trace; print('OK')"
python3 -c "from saver_v3.sft.training import WeightedSFTTrainer; print('OK')"
python3 -c "from saver_v3.teacher.teacher_judge import compute_teacher_judge_weight_multiplier; print('OK')"
```
